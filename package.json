{
  "name": "cleanup",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "npm run generate:seo && npm run generate:sitemap && npm run generate:links-og",
    "build": "vite build",
    "preview": "vite preview",
    "update:tides": "node scripts/update-lancaster-tides.mjs",
    "backfill:w3w": "node scripts/backfill-w3w.mjs",
    "generate:seo": "node scripts/generate-seo-pages.mjs",
    "generate:sitemap": "node scripts/generate-sitemap.mjs",
    "generate:links-og": "node scripts/generate-links-og-image.mjs",
    "predeploy": "npm run update:tides && npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.1",
    "gh-pages": "^6.3.0",
    "playwright": "^1.59.1",
    "vite": "^8.0.1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.99.3",
    "leaflet": "^1.9.4",
    "react-leaflet": "^5.0.0"
  }
}
